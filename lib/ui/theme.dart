import 'package:flutter/material.dart';

const Map colors = {
  'primary': Colors.blue,
  'background': Colors.white,
};

const Map fonts = {
  'primary': 'Roboto',
};

const Map padding = {
  'default': EdgeInsets.all(10),
  'defaultVertical': EdgeInsets.only(top: 10, bottom: 10),
};

const Map borderRadius = {
  'default': BorderRadius.all(Radius.circular(5)),
};